version: '3'
services:
  nginx:
    build: ./nginx
    ports:
      - '80:80'
      - '443:443'
  hm2:
    build: .
    environment:
      WAIT_HOSTS: mongo:27017
#    ports:
#      - '8080:8080'
  mongo:
    image: mongo:4.2.5
    ports:
      - '27017:27017'